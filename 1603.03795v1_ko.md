# 자동 게임 밸런싱의 실현 가능성 입증

Vanessa Volz, Gunter Rudolph, Boris Naujoks

## 초록

게임 밸런싱은 (컴퓨터) 게임 디자인 과정에서 중요한 부분이다. 이 과정에서 디자이너는 게임 프로토타입을 조정하여 결과적으로 나타나는 게임플레이가 가능한 한 재미있도록 만든다. 산업 현장에서 게임 평가는 대개 인간 플레이어를 대상으로 하는 비용 높은 플레이테스트에 기반한다. 따라서 게임플레이와 결과를 예측하는 대체 모델(surrogate model)을 사용해 이 과정을 자동화하는 것이 자연스럽게 제안된다.

본 논문에서는 카드 게임 *Top Trumps*를 대상으로, 시뮬레이션 기반 목적함수와 덱 기반 목적함수를 사용한 자동 밸런싱의 실현 가능성을 조사한다. 또한 알려진 유일한 단일 목적 방법과 비교하여 다목적 접근이 필요한 이유를 주장한다. 우리는 다목적 진화 알고리즘을 적용해, *Top Trumps* 게임의 공정성과 흥미를 표현하기 위해 개발된 목적함수, 예를 들어 승률과 평균 trick 수 등을 최적화하는 덱을 얻는다.

그 결과를 공개된 *Top Trumps* 덱들과 시뮬레이션 기반 목적함수로 비교한다. 이 목적함수 측면에서 공개된 *Top Trumps* 덱보다 더 좋거나 적어도 같은 수준의 덱을 생성할 수 있음을 보인다. 결과는 제시한 접근법을 통한 자동 밸런싱이 실시간 전략 게임과 같은 더 복잡한 게임에서도 실현 가능할 수 있음을 시사한다.

## I. 서론

(컴퓨터) 게임의 복잡성과 인기가 증가하면서 게임 디자이너는 많은 도전에 직면하게 되었다. 특히 게임의 느낌과 필요한 숙련도 양상에 큰 영향을 미치는 게임 메커니즘의 미세 조정은 어려운 작업이다. 예를 들어 디자이너 Jaime Griesemer에 따르면, *Halo 3*에서 저격소총의 발사 간격을 0.5초에서 0.7초로 바꾸는 것만으로도 게임플레이에 큰 영향을 주었다.

게임 디자이너가 가진 게임에 대한 비전은 하나의 의도된 게임 특성만으로 압축되기 어렵다는 점에 주목해야 한다. 예를 들어 경쟁 게임에서는 공정성이 중요하다. 여기서 공정성이란 게임 결과가 운보다 실력에 의존한다는 것, 즉 실력 기반(skill-based)이라는 뜻과, 동일한 실력의 두 플레이어가 대전했을 때 승률이 약 50%에 가깝다는 것, 즉 편향되지 않았다는 뜻을 포함한다. 그러나 동시에 결과가 완전히 결정적이어서는 안 되며, 박빙의 결과를 선호할 수 있는 흥미로운 게임플레이도 제공해야 한다.

따라서 미리 정의된 목표 집합을 충족하는 다양한 게임 파라미터 구성을 자동으로 평가하고 제안할 수 있는 도구로 밸런싱 과정을 지원하는 것이 자연스럽다. 하지만 특정 목표가 설계 시점에 어떤 효과를 낼지는 불분명한 경우가 많다. 따라서 우리는 구성 선택을 잠시 미루고 여러 목표 사이의 절충 관계를 관찰할 수 있게 하는 다목적 접근을 제안한다. 본 논문에서는 게임 밸런싱을 다목적 최적화 문제로 도입하고, 사례 연구를 통해 이 과정의 자동화 가능성을 입증한다.

본 논문에서 게임 밸런싱은 게임의 구성 규칙과 운영 규칙, 즉 기저 물리와 그로 인해 유도되는 결과 또는 피드백의 파라미터를 수정하여, 목표 집합 측면에서 최적의 구성을 달성하는 것으로 정의한다. 이를 위해 카드 게임 *Top Trumps*를 분석하고, 이를 자동으로 밸런싱하는 서로 다른 접근들을 살펴본다. 이를 통해 다목적 접근의 실현 가능성과 장점, 그리고 대체 모델을 도입할 가능성을 보인다.

다음 절에서는 *Top Trumps*, 멀티플레이어 경쟁 게임의 밸런싱, 게임플레이 평가에 관한 관련 연구를 제시한다. 그다음 절에서는 *Top Trumps*와 다목적 최적화에 특화된 중요한 개념들을 설명한다. 이어지는 절에서는 연구 질문을 포함해 본 연구 접근법을 상세히 설명한다. 그 후 분석 결과를 제시하고 논의한 뒤, 결론과 향후 연구 전망으로 마무리한다.

## II. 관련 연구

Cardona 등은 공개 데이터를 바탕으로 *Top Trumps* 게임용 카드를 선택하기 위해 진화 알고리즘을 사용했다 [4]. 그러나 그들의 연구 초점은 게임을 사용해 플레이어가 데이터를 학습하고 데이터에 관해 배울 수 있는 가능성에 있다. 저자들은 주어진 덱의 밸런스를 평가하기 위해 단일 목적의 지배 관계 기반 척도를 개발하고 사용했다. 본 논문에서는 이 척도를 기준점으로 사용한다.

Jaffe는 restricted play라는 기법을 도입했다. 이 기법은 적절히 제한된 에이전트의 승률을 통해 디자이너가 밸런싱 목표를 표현할 수 있도록 하는 것을 목적으로 한다 [11]. 하지만 이 접근은 게임에 대한 전문 지식, AI, 그리고 계산 비용이 클 수 있는 여러 시뮬레이션을 필요로 한다. 반면 본 논문은 설계 목표를 표현하는 다른 가능성과 시뮬레이션에 의존하지 않는 지표를 활용하는 가능성을 탐구한다.

Chen 등은 공진화 프로그래밍을 사용해 대규모 멀티플레이어 온라인 롤플레잉 게임의 밸런스 문제를 해결하려 했다 [5]. 그러나 그들은 레벨 진행에 초점을 두며, 서로 다른 게임 내 캐릭터들의 승률을 같게 하는 것 외의 밸런싱 문제는 다루지 않는다.

게임 구성의 평가를 포함하는 대부분의 연구는 절차적 콘텐츠 생성, 특히 맵 또는 레벨 생성과 관련된다. 여러 논문은 플레이 가능성 [18], 해결 가능성 [17], 다양성 [15, 10] 등에 대한 보장을 제공하는 데 초점을 둔다. 다른 연구 영역으로는 싱글플레이어 게임의 동적 난이도 조정 [9], 규칙 생성 [2, 16], 그리고 mixed-initiative 같은 더 상호작용적인 게임 디자인 방식 [13]이 있다.

## III. 기초 개념

이 절에서는 *Top Trumps* 게임을 소개하고, 적용된 방법인 다목적 최적화와 성능 평가의 이론적 배경을 요약한다.

### 1. Top Trumps

*Top Trumps*는 원래 1970년대에 출시되었고 1999년에 재출시된 테마 카드 게임이다. 인기 있는 테마로는 자동차, 오토바이, 항공기 등이 있다. 덱의 각 카드는 해당 테마의 특정 구성원, 예를 들어 자동차 테마라면 특정 자동차 모델에 해당하며, 가속도, 배기량, 성능, 최고 속도 같은 여러 특성을 표시한다. 예시는 그림 1에서 볼 수 있다.

그림 1: 다섯 개 카테고리를 가진 기차 테마 *Top Trumps* 덱의 카드 예시(winningmoves.co.uk).

게임이 시작되면 덱을 섞고 플레이어들에게 고르게 분배한다. 시작 플레이어는 하나의 특성을 선택하고, 그 값은 다른 플레이어 카드의 해당 특성 값과 비교된다. 가장 높은 값을 가진 플레이어가 해당 trick의 모든 카드를 가져가고, 자신의 다음 카드에서 새 속성을 선택하며 게임을 계속한다. 일반적으로 게임은 적어도 한 플레이어가 모든 카드를 잃으면 끝난다. 그러나 본 논문에서는 게임이 끝나지 않는 문제를 피하기 위해 모든 카드가 한 번씩 플레이된 뒤 게임을 종료한다.

### 2. 다목적 최적화

단일 목적 최적화에서 다목적 최적화로 전환하는 것에는 장단점이 있다 [6]. 기술적 최적화 작업에서는 여러 목적을 고려하는 것이 더 나은 경우가 많지만, 이 경우 개체들 사이의 완전한 순서가 사라진다. 대신 서로 비교 불가능한 해를 다루어야 한다. 예를 들어 두 해가 각각 적어도 하나의 목적에서는 상대보다 더 나은 경우가 있을 수 있다. 이러한 목적별 또는 성분별 접근은 파레토 지배(Pareto dominance)의 정의로 거슬러 올라간다.

해 또는 개체 `x`가 다른 해 `y`를 엄격하게 파레토 지배한다고 할 때, 이는 `x`가 모든 목적에서 `y`보다 더 나은 경우를 뜻한다. 최소화 문제에서 이는 다음과 같이 표현된다.

```text
x ≺ y iff 모든 i in {1, ..., m}에 대해 f_i(x) < f_i(y)
```

적합도 함수는 다음과 같다.

```text
f : X ⊂ R^n -> R^m
f(x) = (f_1(x), ..., f_m(x))
```

이를 바탕으로 파레토 비지배 해 집합, 즉 파레토 집합은 위에서 정의한 비교 불가능한 해들의 집합으로 정의된다. 파레토 프론트는 적합도 함수 아래에서 파레토 집합이 이루는 상이다.

그럼에도 진화 알고리즘에서 선택을 수행하려면 비교 불가능한 해들 사이에서도 구분이 필요하다. 본 논문에서 고려하는 진화 다목적 최적화 알고리즘인 SMS-EMOA [1]에서는 하이퍼볼륨 기여도를 기반으로 이를 수행한다. 하이퍼볼륨은 미리 정의된 기준점에 대해 파레토 프론트가 덮는 목적 공간의 양이다. 전체 프론트의 하이퍼볼륨에 대한 개별 해의 기여도가 `(μ + 1)` 선택 접근의 보조 순위 기준으로 사용된다. 첫 번째 기준은 각 해에 부여되는 비지배 정렬 순위다.

서로 다른 SMS-EMOA 실행의 성능을 측정하기 위해, 결과 파레토 프론트의 하이퍼볼륨 외에도 두 개의 성능 지표를 고려한다. 이는 Knowles 등 [12]이 제시한 additive ε 지표와 R2 지표다. 이 지표들은 Trautmann 등 [19]이 도입한 온라인 수렴 감지를 사용해 EMOA 실행을 종료할 때도 고려된다.

SMS-EMOA의 변이에는 해당 분야에서 가장 널리 사용되는 연산자인 simulated binary crossover와 polynomial mutation을 고려한다. 이들은 Deb [6]에 따라 각각 `p_c = 1.0`, `p_m = 1/n`, `η_c = 20.0`, `η_m = 15.0`으로 매개변수화된다.

### 3. 확률적 다목적 최적화기의 성능 측정

위에서 소개한 단항 성능 지표는 단일 최적화 실행의 성능만 표현한다. 그러나 진화 다목적 알고리즘처럼 결과가 확률적 영향으로 크게 달라질 수 있는 최적화기들의 상대적 성능을 평가하고 비교하려면 통계적 성능 측정이 필요하다.

이를 위해 서로 다른 접근이 달성한 목표 집합을 설명하는 경험적 attainment function이 제안되었다. 이 함수는 달성된 해의 품질뿐 아니라 여러 최적화 실행에 걸친 분포까지 표현한다 [7]. 이 함수를 바탕으로 최적화 실행의 50%, 또는 다른 분위수에서 달성되는 목표 집합을 계산할 수 있으며, 이를 50%-attainment surface라고도 한다. 서로 다른 최적화기의 attainment surface를 비교하는 것은, 확률적 영향을 받는 최상의 해만 비교하는 것보다 훨씬 더 나은 성능 지표다.

또한 Fonseca 등은 두 최적화기의 1차 및 2차 attainment function을 기반으로 하는 통계적 검정 절차를 상세히 설명했다. 이는 양측 두 표본 Kolmogorov-Smirnov 가설 검정과 유사하다 [7]. 이 검정의 귀무가설이 기각되면, 고려한 최적화기들의 성능 차이가 통계적으로 유의하다고 가정할 수 있다.

## IV. 접근법

이후 논문에서는 덱의 카드 수를 `K`라고 하고, 카드에 표시되는 특성 또는 카테고리 수를 `L`이라고 한다. `K`는 짝수다. 덱에는 두 가지 표현을 사용한다. 하나는 진화 알고리즘을 위한 벡터 `x ∈ R^(KL)`이고, 다른 하나는 이해를 쉽게 하기 위한 `K x L` 행렬 `V`다.

이에 따라 k번째 카드의 l번째 카테고리 값은 `v_(k,l)`로 표현하며, `k ∈ {1, ..., K}`, `l ∈ {1, ..., L}`이다. 덱의 k번째 카드는 `v_(k,·) = (v_(k,1), ..., v_(k,L))`이다. 카드들 사이의 부분 순서는 `v_(k1,·) ≻ v_(k2,·)`로 표현할 수 있으며, 이는 `v_(k2,·)` 카드가 모든 카테고리에서 `v_(k1,·)`을 이긴다는 의미다.

본 논문에서는 재미있는 게임플레이에 필수적이라고 보는 두 가지 기본 요건을 충족하는 덱만 고려한다.

- 덱의 모든 카드는 고유해야 한다. 즉 서로 다른 두 카드가 완전히 같은 값을 가져서는 안 된다.
- 덱에는 엄격하게 지배적인 카드가 없어야 한다. 여기서는 게임 규칙상 더 높은 값이 이기므로, 지배적인 카드는 더 큰 값을 가진 카드다.

플레이한 덱에 대해 서로 다른 지식을 가진 두 에이전트 `p4`와 `p0`를 고려한다.

- `p4`는 덱의 모든 카드 값을 정확히 안다.
- `p0`는 모든 값 `v_(k,l)`의 유효 범위만 안다.

두 에이전트는 이미 플레이된 카드를 완벽하게 기억할 수 있다. 균형 잡힌 덱에서는 평균적으로 `p4`가 `p0`보다 더 잘할 것으로 예상된다. 이를 확인하는 데 필요한 시뮬레이션 수를 줄이기 위해, 여기서는 `p4`와 `p0`의 대전만 고려한다.

시뮬레이션에서 두 에이전트는 주어진 카드의 각 카테고리로 이길 확률을 추정한다. 이때 각자의 덱 지식과 이미 플레이된 카드를 고려한다. `p0`는 균등분포를 가정해야 하며, 현재 카드의 값만 고려한다. 반면 `p4`는 아직 플레이 중인 카드 중 각 카테고리에서 더 높은 값을 가진 카드의 수를 고려하여 확률을 더 정확히 모델링할 수 있다.

`R_G`를 시뮬레이션 실행 횟수라고 하자. 덱 `V`로 수행한 r번째 게임이 끝났을 때 `p4`가 얻은 trick 수를 이후 `t_4^(r,V)`라고 부른다. 따라서 `t_4^(r,V) > K/2`이면 `p4`가 게임에서 이긴 것이고, `t_4^(r,V) = K/2`이면 무승부이며, 그 외에는 `p4`가 패배한 것이다. `t_c^(r,V)`는 덱 `V`로 수행한 r번째 게임에서 카테고리를 선택한 플레이어가 해당 trick에서 이기지 못한 횟수, 즉 카테고리를 선언하는 플레이어가 바뀐 횟수다.

최적화 작업은 일반적으로 일반성을 잃지 않고 최소화 문제로 가정되므로, 여기에서도 일관성을 위해 이 관례를 따른다. 따라서 최대화 문제는 `-1`을 곱해 최소화 문제로 변환한다. 본 논문에서는 공개 판매 덱에서 가져온 8개의 카드 집합과, 아래 세 가지 접근 및 그에 대응하는 적합도 함수로 생성한 덱을 비교한다.

첫 번째는 Cardona 등 [4]이 제안한 지배 관계 기반 척도 `D`에 따른 단일 목적 최적화다. 이 척도는 덱 `V`에 있는 카드들이 파레토 프론트로부터 얼마나 떨어져 있는지를 설명한다.

```text
f_D(V) = - (1/K) * sum_k sum_i (1 - 1(v_k ≺ v_i))
```

두 번째는 전문가 지식을 바탕으로 개발된 시뮬레이션 기반 척도를 사용하는 다목적 최적화다. 이는 덱 `V`의 공정성, 흥미, 그리고 결과적 밸런스를 표현하기 위한 것이다. 이를 `B`라고 한다.

```text
f_B(V) =
(
  - (1/R_G) * sum_r 1(t_4^(r,V) > K/2),
  - (1/R_G) * sum_r t_c^(r,V),
    (1/R_G) * sum_r |2t_4^(r,V) - K/2|
)
```

직관적으로 말하면 이 목적들은 `p4`의 승률, 주도권이 바뀌는 횟수, 그리고 게임 결과가 얼마나 접전인지와 관련된다.

세 번째는 사전 실험 계획 단계에서 개발된 시뮬레이션 독립 척도를 사용하는 다목적 최적화다. 이는 서로 다른 덱의 시뮬레이션 기반 적합도 `f_B`를 대신할 대체 척도 `S`로 사용된다.

```text
f_S(V) = (-hv(V), -sd({avg(v_(·,l)) | l in {1, ..., L}}))
```

여기서 `hv`는 덱 `V`의 dominated hypervolume, `sd`는 경험적 표준편차, `avg`는 평균을 뜻한다.

앞서 언급한 접근들을 비교하기 위해, 적합도 함수 `f_S`와 `f_B`에 대해서는 SMS-EMOA를 사용해 파레토 프론트를 근사한다. 온라인 수렴 감지, 변이 연산자, 매개변수는 앞 절에서 설명한 것을 사용한다. 단일 목적 적합도 `f_D`에 대해서는 비교가 가능하도록 알고리즘을 가능한 한 적게 수정했다. 따라서 같은 변이 연산자와 동등한 선택을 사용하는 `(μ + 1)-EA`를 사용했다. 수렴은 활성 집단의 최소, 평균, 최대 적합도 값이라는 단일 목적 성능 지표들의 변화를 바탕으로 검사했다. 실험은 R에서 `emoa` 패키지와 관련 SMS-EMOA 구현을 사용해 수행했다.

### 1. 연구 질문

균형 잡힌 *Top Trumps* 덱을 찾기 위한 서로 다른 접근들은 V절에서 평가하고 비교한다. 본 논문은 다음 주제에 초점을 둔다.

1. 수동 밸런싱의 문제와 자동화가 제공하는 해결책
2. 필요한 품질 측면에서 자동 밸런싱의 실현 가능성
3. 다목적 접근과 단일 목적 접근의 성능
4. 계산 비용 측면에서 자동 밸런싱의 실현 가능성

### 2. 사전 실험 계획

실험을 수행하기 전에 테스트 사례를 더 정확히 정의해야 한다. 다음 가정을 둔다.

- 카드 수와 카테고리 수는 공개 판매 덱의 값에 맞추어 `K = 32`, `L = 4`로 설정한다.
- 모든 값 `v_(l,k)`의 유효 범위는 `[1, 10] ⊂ R`로 설정한다. 모든 덱은 이 범위로 변환될 수 있다. 이는 가능한 카드의 수가 무한하다는 결과를 낳지만, 다른 선택지는 유전자형-표현형 매핑을 구성해야 하는 필요를 동반한다.

플레이어 사이의 가능한 카드 분배, 덱 안의 카드 순서, 서로 다른 시작 플레이어의 수가 매우 크기 때문에, 하나의 덱도 잠재적으로 많은 서로 다른 게임, 즉 `4K!`개의 게임으로 이어질 수 있다. 따라서 덱을 평가하는 모든 시뮬레이션 기반 지표는 근사되어야 한다. `f_B`의 지표들은 모두 평균을 표현하므로, 각 분포의 참 평균에 가능한 한 가까워야 한다.

근사의 품질을 보장하기 위해, 신뢰수준 0.95에서 `R_G`가 100에서 10,000 사이일 때 각 지표의 신뢰구간 크기를 계산하는 통계적 t-검정을 수행한다. 이 검정은 가능한 각 표본 크기와 각 지표에 대해 500번 반복된다. 정규분포를 가정하고, 0.95 분위수를 결과로 저장한다. 모든 지표에서 계산 시간과 적합도 근사 정확도 사이의 좋은 절충점으로 `R_G = 2,000` 게임이 선택되었다.

각 접근의 성능을 적절한 신뢰구간으로 근사하기 위해 필요한 최적화 실행 횟수를 결정하는 동등한 검정도 수행한다. 여기서는 모든 실행에서 얻은 파레토 프론트를 기준 집합으로 삼아 HV, ε, R2 지표를 고려한다. 결과를 고려한 뒤 실행 횟수 `R_O`는 100으로 설정했다.

시뮬레이션 독립 접근에 대해서는 시뮬레이션을 필요로 하지 않는 지표를 개발한다. 하이퍼볼륨은 각 덱에서 가능한 한 많은 비지배 카드를 얻기 위한 척도로 선택된다. 이는 덱의 공정성을 향상할 것으로 기대된다. 카테고리 평균의 표준편차는 플레이어 `p0`의 불리함이 더 중요하게 작용하도록 하여, 결과적으로 `p4`의 승률을 높이는 데 사용된다.

서로 다른 집단 크기를 테스트하고, 빠른 성능 추정을 위해 그 결과 파레토 프론트를 비교한다. 얻은 결과를 바탕으로, 접근들은 집단 크기 10과 100에서 평가된다. 이는 높은 선택 압력을 가진 작은 집단과 더 넓은 분포를 가진 큰 집단을 모두 고려하기 위한 것이다.

## V. 결과

우리는 모든 접근을 적합도 함수 `f_B`에 따라 평가한다. `f_B`는 전문가 지식에 기반하므로 균형 잡힌 덱을 가장 잘 특성화한다고 가정한다. 이 가정은 대부분의 공개 판매 덱이 이 지표에 따른 근사 파레토 프론트에 위치한다는 사실에 의해 뒷받침된다.

이후 논문에서는 다목적 접근에 대해, 해당 적합도 함수 문자와 집단 크기를 사용해 `R_O = 100`번 실행에서 얻은 파레토 프론트의 합집합을 지칭한다. 뒤에 `p`가 붙은 약어는 `f_B`에 대한 해당 집합의 파레토 프론트를 뜻한다. 숫자 인덱스는 해당 수준의 attainment surface를 의미한다. 예를 들어 `S10`은 적합도 함수 `f_S`와 집단 크기 10으로 실행한 최적화기의 모든 파레토 프론트 합집합이고, `S10p`는 이 집합의 파레토 프론트, `S1050`은 이 집합의 50%-attainment surface다. 단일 목적 접근에서는 집단이 하나의 덱으로 수렴하므로, `R_O = 100`번 실행에서 얻은 최상의 개체들의 합집합을 대신 고려한다. 공개 판매 덱 집합은 이후 `PD`라고 부른다.

실험 논의를 쉽게 하기 위해, 세 접근의 결과는 적합도 함수 `f_B`에서의 성능으로 표시한다. 그림 2는 표 1에 나열된 집합들을 나타낸다. 그림 3은 각 접근에서 파레토 프론트 합집합으로부터 얻은 50%-attainment surface와 파레토 프론트를 시각화한다. 모든 그림의 범례는 표 1에 있으며, 각 접근의 파레토 프론트와 attainment surface를 같은 색 구성으로 표시한다.

표 1: 그림 2와 그림 3에 표시된 서로 다른 최적화기 실행 결과의 색상 배정 범례. 같은 접근의 모든 결과, 예를 들어 `B10`, `B1050`, `B10p`에는 같은 색이 배정된다.

```text
B10   D10   S10   PD
B100  D100  S100
```

### 1. 자동 밸런싱의 장점

자동 밸런싱의 장점을 평가하기 위해 다음 가설을 제안한다.

- I-C1: 하나의 덱에 대해 일부 시뮬레이션 기반 지표를 적절한 정확도로 근사하는 데 필요한 테스트 수는 매우 많으며, 인간 플레이어로 합리적으로 수행 가능한 플레이테스트 수를 초과할 수 있다.
- I-C2: 공개 판매 덱 중 상당수는 게임 결과가 플레이어의 실력보다 운에 강하게 의존한다는 의미에서 불공정하다.

이 가설들을 통해 수동 밸런싱에 필요한 노력을 고려하고, `PD`, 즉 수동으로 밸런싱되었을 가능성이 높은 덱들의 성능을 자동 밸런싱 덱과 비교한다.

II절에서 설명한 t-검정은 시뮬레이션 기반 지표 근사의 정확도와 시뮬레이션 수 사이의 최적 절충점이 `R_G ≈ 2,000`임을 이미 결정했다. 인간을 대상으로 한 플레이테스트는 큰 노력을 필요로 하며, 서로 다른 플레이어가 플레이함으로써 생기는 편향도 고려해야 한다. 따라서 인간으로 2,000판을 테스트하는 것은 지루하고, 예산이 작은 경우에는 불가능할 수도 있다. 예를 들어 `B10`의 덱들에 대해 `f_B`의 표준편차는 약 `(0.0427, 0.3191, 0.3576)`이다. 100명의 플레이어가 각각 10판씩 플레이한다고 가정하면, `α = 0.05`에서 얻어지는 신뢰구간은 약 `(0.0442, 0.4298, 0.15)`가 된다. 이는 디자이너가 서로 다른 해를 구분하기에 충분히 정확하지 않다.

표준편차와 필요한 게임 수는 게임의 복잡성이 증가함에 따라 더 커질 가능성이 높다. 따라서 수동 플레이테스트에 비해 자동 밸런싱이 갖는 명확한 장점은 시뮬레이션 기반 지표를 정량적으로 분석할 수 있다는 것이다 [11].

오토바이 덱 하나를 제외하면, 공개 판매 덱들은 모두 추정된 파레토 프론트의 가장자리, 특히 `p4`의 승률 측면에서 가장 나쁜 성능 영역에 위치한다. 이는 그림 3에서 뚜렷하다. `p4`의 낮은 승률은 아마도 `PD`에 있는 해당 덱들의 비지배 카드 수가 상대적으로 적기 때문일 것이다. `f_D` 평균은 약 `-24.12`로, 모든 카드가 비지배 카드인 최적값 `-31`과 비교된다. 이는 결과적 게임플레이가 운에 크게 의존함을 의미한다. 어떤 카드 조합에서는 플레이어의 실력과 관계없이 단순히 이길 수 없기 때문이다. 유일한 예외는 오토바이 덱으로, `f_D` 값이 `-30.4375`이고 `p4` 승률도 약 0.8로 훨씬 좋다.

따라서 하나의 덱을 평가하는 데 필요한 노력이 합리적인 플레이 횟수를 넘어선다는 점을 보였다. 또한 수동으로 밸런싱된 덱들이 파레토 프론트의 극단 가장자리에 위치한다는 것은, 수동으로는 덜 극단적인 해를 찾기 어렵다는 뜻이다. 이는 파레토 프론트의 근사가 디자이너에게 게임 특성과 가능한 대안에 대한 더 정교한 이해를 제공함으로써 도움을 줄 수 있음을 시사한다. Nelson과 Mateas의 연구도 디자이너들이 밸런싱 과정을 지원하는 자동 밸런싱 도구의 가능성을 본다는 점을 시사한다 [14].

### 2. 자동 밸런싱의 품질

다음으로 자동 밸런싱의 실현 가능성, 즉 자동으로 밸런싱된 덱 중 적어도 일부가 공개 판매 덱과 동등한 성능을 보인다는 점을 입증한다.

- II-C1: 자동으로 밸런싱된 덱은 파레토 프론트 위에 있다.
- II-C2: II-C1의 결과는 통계적으로 유의하다.

그림, 특히 그림 3의 오른쪽 그림에서 명확하듯이, 자동 밸런싱 덱들(`S10`, `B10`)은 파레토 프론트의 큰 부분을 차지하며, 따라서 공개 판매 덱에 의해 지배되지 않는다. 또한 대부분의 공개 판매 덱은 프론트의 극단 가장자리에 집중되어 있다.

`S1050`과 `B1050`의 개체들에 대해서도 같은 점이 성립한다. 이는 접근법의 확률성에도 불구하고, 전체 최적화 실행 중 적어도 50%에서 파레토 프론트 위의 덱을 얻을 수 있음을 보장하므로, 결과를 통계적으로 의미 있게 만든다.

### 3. 단일 목적 접근과 다목적 접근의 성능

Cardona 등 [4]이 사용한 접근을 다목적화하면 더 나은 개체를 얻을 수 있는지 분석한다. 이를 위해 `f_D`를 `f_S`로 확장한다. `f_S`의 첫 번째 부분인 dominated hypervolume이 클수록 덱 안에 더 많은 비지배 카드가 있다는 뜻이며, 이는 `f_D`가 표현하는 바와 관련된다. 하이퍼볼륨은 또한 더 넓게 퍼진 카드를 선호한다. 이러한 분포는 지배 관계, 플레이스루 결과, 시뮬레이션 기반 적합도 값에는 영향을 주지 않는다. `f_S`의 두 번째 부분은 카테고리 평균의 표준편차다. 이 편차가 클수록, 지식 부족을 보완하기 위해 카테고리에 균등분포를 가정하는 `p0`의 전략은 더 문제가 된다.

- III-C1: 고려한 다목적 접근(`S10`, `S100`)과 단일 목적 접근(`D10`, `D100`)의 경험적 attainment function 사이에는 유의한 차이가 있다.
- III-C2: 단일 목적 접근(`D10`, `D100`)의 결과는 `f_B` 측면에서 다목적 접근(`S10`, `S100`)보다 유의하게 나쁘다.

III-C1을 검정하기 위해, Fonseca 등 [7]이 설명한 경험적 attainment surface 비교를 위한 통계적 검정 절차를 수행했다. `10,000`개의 무작위 변이와 `α = 0.05`에서, 두 접근의 attainment function이 동일하게 분포한다는 귀무가설은 `{D10, D100} x {S10, S100}`의 모든 비교에서 `p-value = 0`으로 기각된다. 이는 그림 2 같은 시각화에서 볼 수 있듯, 분석된 접근들이 찾은 개체들이 매우 다른 영역에 있기 때문에 예상된 결과였다. 또한 단일 목적 접근이 찾은 결과는 분포가 매우 좁은데, 이는 아마 `f_D` 적합도 척도의 성격 때문일 것이다.

단일 목적 접근으로 찾은 해 집합들은 Knowles 등 [12]의 정의에 따라 두 대체 접근 모두에 의해 엄격히 지배된다. 형식적으로는 다음 관계가 성립한다.

```text
(D1050 ∪ D10050) ≺ (D10 ∪ D100) ≺ S1050 ≺ S10
(D1050 ∪ D10050) ≺ (D10 ∪ D100) ∥ S10050 ≺ S100
```

III-C1에 대한 검정은 접근들의 attainment function이 동일하지 않음을 보였다. 이는 `f_D` 대신 `f_S` 적합도 함수를 사용한 것이 `f_B` 측면에서 결과를 향상했음을 나타내며, 따라서 III-C2를 확인한다. 이 사례에서는 `f_D`의 다목적화가 실제로 결과를 개선할 수 있음을 시사한다.

### 4. 계산 비용과 대체 목적함수

이제 계산 비용 측면에서 자동 밸런싱의 실현 가능성을 다룬다. 앞 절에서는 `f_B` 함수에서 실현 가능성을 이미 분석하고 검증했다. 따라서 고려한 응용에서는 게임당 `R_G = 2,000`회 시뮬레이션과 `R_O = 100`번 최적화 실행에 필요한 계산 비용이 관리 가능한 수준임이 분명하다.

하지만 더 복잡한 게임에서는 시뮬레이션 기반 밸런싱 접근이 너무 비쌀 수 있다. 특히 계산 비용이 큰 시뮬레이션이나 큰 게임 상태 공간을 가진 게임에서는 더 그렇다. 우리는 `f_B` 대신 `f_S` 같은 시뮬레이션 독립 척도를 사용할 가능성을 조사함으로써 이 문제에 접근한다. 실제로는 이러한 척도들이 의도한 밸런싱 목표와 최적화기의 행동 관찰에 맞추어 개발되어야 한다.

자동 밸런싱의 계산 비용과 시뮬레이션 독립 목적함수의 실현 가능성을 조사하기 위해 다음 가설들을 제시한다.

- IV-C1: `f_S` 적합도 함수로 최적화한 일부 결과(`S10`, `S100`)는 `B10`, `B100`에 의해 지배되지 않는다.
- IV-C2: `S10`과 `S100`의 최상의 개체들은 성능 지표 측면에서 `B10`, `B100`의 개체들과 적어도 동등하게 수행한다.
- IV-C3: `S10`, `S100`과 `B10`, `B100`의 attainment function 사이에는 유의한 차이가 없다.
- IV-C4: IV-C1과 IV-C2의 결과는 통계적으로 유의하다.

그림 2와 그림 3에서 볼 수 있듯이, `S10`에는 공유 파레토 프론트 위에 있는 개체들이 있으며, 따라서 `B10 ∪ B100`의 어떤 개체에도 지배되지 않는다. 실제로 `B100 ≺ S10`이고 `B10 ∥ S10`이다. `S100`에 대해서는 `S100 ∥ B100`이라고만 말할 수 있으므로, IV-C1은 `S10`에 대해서만 참이다.

여기서 고려한 접근들의 파레토 프론트 성능을 비교하기 위해 `S10p`, `S100p`, `B10p`, `B100p`에 대해 HV, ε, R2 성능 지표를 계산한다. 이 값들의 해석을 돕기 위해, 앞서 언급한 집합들은 얻어진 모든 값에 대해 정규화된다. 그 결과 값은 1과 2 사이가 된다 [19]. 모든 달성된 프론트의 합집합에서 얻은 정규화된 파레토 프론트를 기준 집합으로 사용한다. 결과 지표 값은 표 2의 위쪽 절반에 있다. 표 2 위쪽 절반의 비지배 정렬 순위는 계산된 값에 대해 II-C2가 참이며, 같은 집단 크기의 접근들이 동등하게 잘 수행한다는 점을 분명히 보여준다.

표 2: 고려한 접근의 파레토 프론트와 50%-attainment surface에 대한 정규화된 지표 값과 순위. 괄호 안 순위 값은 통계적으로 유의하지 않은 구분을 고려한 대안 순위다.

| 집합 | HV | ε | R2 | 순위 |
| --- | ---: | ---: | ---: | ---: |
| B10p | 0.241 | 0.329 | 0.103 | 1 |
| B100p | 0.541 | 0.559 | 0.203 | 2 (3) |
| S10p | 0.177 | 0.368 | 0.091 | 1 |
| S100p | 0.475 | 0.501 | 0.206 | 2 |
| B1050 | 0.490 | 0.555 | 0.203 | 1 (1) |
| B10050 | 0.638 | 0.650 | 0.258 | 3 (2) |
| S1050 | 0.527 | 0.565 | 0.222 | 2 (1) |
| S10050 | 0.676 | 0.692 | 0.288 | 4 (3) |

이 진술의 통계적 유의성을 검정하기 위해, 각 집합과 각 지표에 대해 `α = 0.05`에서 신뢰구간의 폭 `w`를 계산한다. 이는 각 접근의 `R_O = 100`회 실행에서 별도로 얻어진 성능 지표에 대해 참 지표 평균을 추정하는 t-검정으로 수행되며, 앞서와 같이 정규화된다. 신뢰구간이 표현하는 불확실성을 고려하면, 표 2의 위쪽 절반에 있는 성능 지표 차이들은 `B10`과 `S10`, 그리고 `B100`과 `S100`의 R2 차이를 제외하고 모두 통계적으로 유의하다. 이는 참 순위에서 `B100`이 2가 아니라 3일 수도 있음을 뜻한다.

III-C1 가설에서 설명한 경험적 attainment function 비교 검정은 여기서 다시 `B10`, `B100`, `S10`, `S100`의 모든 조합을 비교하는 데 적용된다. IV-C3과 달리, 검정은 모든 경우에서 `p-value = 0`으로 동일한 attainment function이라는 귀무가설을 기각한다. 다만 III절보다 판단은 조금 더 근소하다. 따라서 IV-C3은 확인될 수 없다. attainment function의 차이는 비교한 집합들이 목적 공간의 서로 다른 영역을 차지하기 때문일 가능성이 높다. 이는 `f_S`가 플레이스루의 흥미를 표현하지 못했기 때문으로 설명할 수 있다. `f_S`는 `f_D`에서 출발해 덱의 공정성을 더 잘 표현하도록 구성되었기 때문이다. 따라서 `f_B`로 얻은 해를 시뮬레이션 독립 적합도 척도로 근사하는 것이 목표라면, 다른 척도를 선택해야 한다. 앞서 언급한 검정의 p-value를 그 품질의 지표로 사용할 수도 있다.

그림 3의 왼쪽에서 보듯이, `B1050`과 `S1050`은 모두 공유 파레토 프론트 위의 개체들을 포함한다. 이는 적어도 50%의 최적화 실행에서 IV-C1이 참임을 입증한다. 각 접근에 대한 50%-attainment surface의 성능 지표는 표 2의 아래쪽 절반에 있으며, 순위와 불확실성을 고려했을 때 가능한 참 순위도 함께 제시된다. 이 경우 `S10050`은 고려한 다른 모든 접근보다 유의하게 나쁘다. 그러나 `S1050`은 `B10050`보다 확실히 더 잘 수행하며, `S1050`과 `B1050`의 성능 사이에는 명확한 순위가 없다. 이는 IV-C4도 참임을 시사한다.

표 2의 값들은 모두 정규화된 결과에 기반하므로 절댓값을 비교할 수 있다. 예상대로 모든 50%-attainment surface는 대응하는 파레토 프론트보다 나쁘게 수행하며, 차이는 유의하다. 흥미롭게도 각 지표에서의 차이는 표 2의 아래쪽 절반에서 더 작다. 이는 그림 3의 왼쪽에서 서로 다른 접근의 50%-attainment surface 간 목적 공간 거리들이, 그림 3의 오른쪽에 있는 파레토 프론트들 사이의 거리보다 눈에 띄게 작다는 사실을 반영한다. 또한 `S10050`과 `B10050`에는 각각 `S100p`와 `B100p`보다 더 많은 개체가 있으며, 이는 성능 지표 손실이 더 작은 이유를 설명한다. 두 접근 모두 최적 방향으로의 분산이 더 작기 때문이다.

### 5. 추가 관찰

앞서 논의한 결과 외에도 실험 중 몇 가지 흥미로운 관찰이 있었다.

단일 목적 최적화 접근은 테스트한 두 집단 크기 모두에서 하나의 덱으로 수렴한다. 이는 오직 비지배 카드로만 구성된 모든 덱이 동일하게 좋은 성능을 보임에도 그렇다. `f_D`의 최적 적합도 값인 31은 거의 모든 실행에서 달성된다. 이는 수렴 감지를 포함한 단일 목적 최적화 알고리즘이 이 응용에서는 작동했음을 시사한다. 더 나아가 `f_D`는 덱들을 잘 구분하지 못하므로 덱 생성에는 적합하지 않다고 결론낼 수 있다. Cardona 등 [4]이 수행한 데이터 선택의 경우에는 전혀 다를 수 있다.

최적화기 실행은 같은 접근에서도 매우 다른 수의 함수 평가 `n_eval` 이후 수렴 감지 메커니즘에 의해 중단되었다. 예를 들어 `S10`의 처음 30회 실행은 3,727회에서 23,737회 사이의 적합도 함수 평가를 수행했다. 달성한 해의 품질과 `n_eval` 사이에는 뚜렷한 상관관계가 없었다. `S10`의 이 부분집합에서 파레토 프론트 위의 해를 가진 실행들의 `n_eval`은 3,993에서 20,243 사이였다. 이는 적합도 함수 지형의 복잡도가 높음을 가리키며, 이 실험에서 온라인 수렴 감지를 사용하는 것을 정당화한다.

## VI. 결론과 전망

본 논문에서는 자동 게임 밸런싱에 대한 접근을 제시하고, 이를 카드 게임 *Top Trumps*에 적용했다. 이 접근은 문제를 다목적 최소화 문제로 형식화하고 해석하는 것을 포함하며, 온라인 수렴 감지를 갖춘 최신 EMOA를 사용해 이를 해결한다. 생성된 덱과 공개 판매 덱, 그리고 단일 목적 접근 [4]의 성능은 통계 분석을 사용해 평가되었다.

우리는 IV절에서 상세히 설명한 가정 아래, *Top Trumps* 게임에서 달성된 해의 품질 측면에서 자동 게임 밸런싱의 실현 가능성을 결론적으로 보였다. 계산 비용 문제가 복잡한 응용에서 시뮬레이션 기반 접근을 불가능하게 만들 수 있음을 인식하고, 시뮬레이션을 피하는 접근도 IV절에서 개략적으로 제시했다. 따라서 본 연구는 일반적인 자동 밸런싱의 실현 가능성을 입증하기 위한 필요한 단계다. 또한 자동 밸런싱 접근과 다목적 밸런싱의 명백한 장점도 V절에서 논의했다. 이러한 논의와 V절의 추가 관찰들은 제시된 접근이 적합하고 성공적이었음을 강하게 시사한다.

향후 이 연구를 진행할 한 가지 방법은 접근의 여러 부분을 더 최적화하는 것이다. 예를 들어 고려한 최적화기는 순차적 파라미터 최적화 같은 튜닝 방법으로 더 나은 파라미터를 결정함으로써 개선될 수 있으며, 그 결과를 더 선명하게 만들 수 있다. 또한 온라인 수렴 감지 메커니즘 같은 여러 모듈에 대해 가능한 파라미터 개선을 테스트해야 한다.

구현된 플레이어 AI와 관련해서는, 본 연구에서 사용한 확률적 AI의 여러 개선을 테스트함으로써 연구를 확장하는 것이 타당해 보인다. 이는 정확히 `K/2`라운드만 플레이하도록 허용하는 제한이 제거될 경우 흥미로운 결과를 제공할 수 있다. 그 경우 에이전트는 앞을 내다봐야 하며 더 복잡한 전략이 이익을 낼 수 있다. 가능한 AI 확장으로는 Buro 등이 trick 기반 카드 게임에서 상태 평가 개선에 관한 연구에서 보인 것처럼 상대 카드에 대한 추론 기반 reasoning이 있다. 카드 게임은 불완전 정보를 포함하는 경우가 많으므로 Monte Carlo Search도 흔히 사용된다 [8, 20]. 또 다른 경로는 인간 플레이어를 모방하는 AI를 구현하는 것이다.

제시한 척도에 대한 추가 분석과 새로운 척도 발견도 계획하고 있다. 이 방향의 첫 단계로, 응용에 특화된 방법을 개발한 뒤 우리의 접근을 다른 응용에 사용하는 것을 제안한다. 이와 관련하여 더 복잡한 컴퓨터 게임에서 접근을 테스트하는 것을 목표로 한다. 첫 시도는 The Open Racing Car Simulator(TORCS)를 포함하는 방식으로 이루어질 것이며, 실시간 전략 게임과 플랫폼 게임에 대한 추가 테스트도 계획하고 있다. 잘 작동하는 여러 적합도 척도를 분석한 뒤, 다음 단계로 일반화 가능한 척도를 조사할 수 있다.

더 중요하게는, 우리는 균형 잡힌 덱에 대한 우리의 관점, 적합도 척도, 그리고 방법의 결과를 인간 플레이어 설문으로 평가할 계획이다. 우리의 의견으로는, 밸런싱에 대한 인간의 인식을 포함하는 것이 최종 목표, 즉 인간 플레이어의 게임 즐거움을 정확히 표현하고 최대화하는 목표를 달성하는 유일하게 받아들일 수 있는 방법이다.

## 참고문헌

[1] N. Beume, B. Naujoks, and M. Emmerich. "SMS-EMOA: Multiobjective Selection Based on Dominated Hypervolume." *European Journal of Operational Research*, 181(3):1653-1669, 2007.

[2] C. B. Browne. *Automatic generation and evaluation of recombination games*. PhD thesis, Queensland University of Technology, 2008.

[3] M. Buro, J. R. Long, T. Furtak, and N. R. Sturtevant. "Improving state evaluation, inference, and search in trick-based card games." In *Conference on Artificial Intelligence (IJCAI)*, pp. 1407-1413. Morgan Kaufmann, San Francisco, CA, 2009.

[4] A. B. Cardona, A. W. Hansen, J. Togelius, and M. G. Friberger. "Open Trumps, a Data Game." In *Foundations of Digital Games (FDG 2014)*. Society for the Advancement of the Science of Digital Games, Santa Cruz, CA, 2014.

[5] H. Chen, Y. Mori, and I. Matsuba. "Solving the balance problem of massively multiplayer online role-playing games using coevolutionary programming." *Applied Soft Computing*, 18:1-11, 2014.

[6] K. Deb. *Multi-Objective Optimization Using Evolutionary Algorithms*. Wiley, Chichester, UK, 2001.

[7] C. Fonseca, V. D. Fonseca, and L. Paquete. "Exploring the performance of stochastic multiobjective optimisers with the second-order attainment function." In C. A. C. Coello et al., editors, *Evolutionary Multi-Criterion Optimization (EMO 2005)*, pp. 250-264. Springer, Berlin, 2005.

[8] T. Furtak and M. Buro. "Recursive Monte Carlo search for imperfect information games." In *Computational Intelligence and Games (CIG 2013)*, pp. 225-232. IEEE Press, Piscataway, NJ, 2013.

[9] G. Hawkins, K. V. Nesbitt, and S. Brown. "Dynamic Difficulty Balancing for Cautious Players and Risk Takers." *International Journal of Computer Games Technology*, 2012:1-10, 2012.

[10] A. Isaksen, D. Gopstein, J. Togelius, and A. Nealen. "Discovering Unique Game Variants." In H. Toivonen et al., editors, *Computational Creativity (ICCC 2015)*. Brigham Young University, Provo, Utah, 2015.

[11] A. Jaffe. *Understanding Game Balance with Quantitative Methods*. PhD thesis, University of Washington, 2013.

[12] J. Knowles, L. Thiele, and E. Zitzler. *A Tutorial on the Performance Assessment of Stochastic Multiobjective Optimizers*. TIK Report 214, Computer Engineering and Networks Laboratory (TIK), ETH Zurich, 2006.

[13] A. Liapis, G. N. Yannakakis, and J. Togelius. "Sentient sketchbook: Computer-aided game level authoring." In *Foundations of Digital Games (FDG 2013)*, pp. 213-220. Society for the Advancement of the Science of Digital Games, Santa Cruz, CA, 2013.

[14] M. J. Nelson and M. Mateas. "A requirements analysis for videogame design support tools." In *Foundations of Digital Games (FDG 2009)*, pp. 137-144. ACM Press, New York, 2009.

[15] M. Preuss, J. Togelius, and A. Liapis. "Searching for Good and Diverse Game Levels." In *Computational Intelligence and Games (CIG 2014)*, pp. 381-388. IEEE Press, Piscataway, NJ, 2014.

[16] A. M. Smith and M. Mateas. "Variations Forever: Flexibly generating rulesets from a sculptable design space of mini-games." In *Computational Intelligence and Games (CIG 2010)*, pp. 273-280. IEEE Press, Piscataway, NJ, 2010.

[17] A. M. Smith, E. Butler, and Z. Popovic. "Quantifying over Play: Constraining Undesirable Solutions in Puzzle Design." In *Foundations of Digital Games (FDG 2013)*, pp. 221-228. Society for the Advancement of the Science of Digital Games, Santa Cruz, CA, 2013.

[18] J. Togelius, M. Preuss, N. Beume, S. Wessing, J. Hagelback, G. N. Yannakakis, and C. Grappiolo. "Controllable procedural map generation via multiobjective evolution." *Genetic Programming and Evolvable Machines*, 14(2):245-277, 2013.

[19] H. Trautmann, T. Wagner, B. Naujoks, M. Preuss, and J. Mehnen. "Statistical Methods for Convergence Detection of Multi-Objective Evolutionary Algorithms." *Evolutionary Computation*, 17(4):493-509, 2009.

[20] C. D. Ward and P. I. Cowling. "Monte Carlo search applied to card selection in Magic: The Gathering." In *Computational Intelligence and Games (CIG 2009)*, pp. 9-16. IEEE Press, Piscataway, NJ, 2009.

